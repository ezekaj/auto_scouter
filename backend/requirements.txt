# EMERGENCY ULTRA-MINIMAL RUST-FREE REQUIREMENTS
# Absolute minimum packages with guaranteed binary wheels

# Core FastAPI - Older stable versions with NO Rust dependencies
fastapi==0.95.1
uvicorn==0.21.1
python-multipart==0.0.5

# Database - Minimal setup
sqlalchemy==1.4.48
psycopg2-binary==2.9.5

# Data validation - Pydantic v2 with binary wheels (verified working)
pydantic==2.3.0
pydantic-settings==2.0.3

# Authentication - Pure Python only
PyJWT==2.6.0
passlib==1.7.4

# Deployment
gunicorn==20.1.0
